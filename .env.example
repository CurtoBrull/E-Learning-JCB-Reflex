# MongoDB Configuration
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/database?retryWrites=true&w=majority&appName=cluster-name

# API Configuration
API_URL=http://localhost:8000

# Browser Configuration (for reflex login and opening URLs)
# WSL/Linux: Use Windows Edge path
BROWSER=/mnt/c/Program Files (x86)/Microsoft/Edge/Application/msedge.exe
# Native Linux: Use browser command (google-chrome, firefox, etc.)
