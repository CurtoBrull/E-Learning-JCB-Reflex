# Variables de entorno para producción en Reflex Deploy
# Copia este archivo a .env.production y actualiza los valores

# Entorno
ENVIRONMENT=production

# MongoDB Atlas (REQUERIDO)
# Obtén tu connection string desde MongoDB Atlas Dashboard
MONGODB_URI=mongodb+srv://<usuario>:<password>@<cluster>.mongodb.net/<database>?retryWrites=true&w=majority

# URLs de la aplicación (se configuran automáticamente por Reflex Deploy)
# Solo necesitas configurarlas si usas un dominio personalizado
# API_URL=https://tu-app.reflex.run
# FRONTEND_URL=https://tu-app.reflex.run

# Puertos (configurados por defecto, no es necesario cambiar)
# BACKEND_PORT=8000
# FRONTEND_PORT=3000
